## Summary

What this change does and why.

## Related issue

Closes #

## Changes

-

## Testing

- [ ] `pytest` passes (coverage stays at or above the gate)
- [ ] `ruff check .` and `ruff format --check .` are clean
- [ ] `mypy src/memory_fabric` is clean
- [ ] Added or updated tests for the changed behavior

## Checklist

- [ ] Updated `CHANGELOG.md` under `[Unreleased]` (if user-visible)
- [ ] Updated docs affected by this change
- [ ] If the version was bumped, README / ROADMAP / `server.json` all agree
      (`tests/test_version_truth.py`)
- [ ] No telemetry, no network calls added to core read/write paths
